{% extends 'base.html.twig' %}

{% block meta_description %}Homepage{% endblock %}

{% block title %}Homepage{% endblock %}

{% block body %}

<div class="d-flex">
<div class="d-flex flex-column flex-shrink-0 p-3 text-white bg-secondary shadow" style="width: 280px; height: 90vh; position:absoulte;">

    <a href="#" class="d-flex align-items-center mb-3 mb-md-0 me-md-auto text-decoration-none">
      {# <img src="{{ asset('build/images/me.png') }}" width="70%" class="mt-3"> #}
      <img src="https://picsum.photos/200" width="70%" class="mt-3">
    </a>

    <hr>
    <ul class="nav nav-pills flex-column mb-auto">

    {% for category in categories %}
      <li class="shadow nav-item fw-bold p-2 mb-1 rounded">
        <a href="#" class="nav-link text-light" aria-current="page">
        {# <a href="{{ path('app_category_show', {'id': category.id}) }}" class="nav-link text-warning" aria-current="page"> #}
          {{ category.name }}
        </a>
      </li>
    {% endfor %}
</div> 

{% endblock %}
